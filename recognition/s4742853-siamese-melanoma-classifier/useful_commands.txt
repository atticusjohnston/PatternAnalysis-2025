ssh <id>@rangpur.compute.eait.uq.edu.au
squeue -u $USER -o "%.10i %.9P %.20j %.2t %.10M %.6D %R"
scontrol show job <jobid>
sacct -j <jobid>
scp <id>@rangpur.compute.eait.uq.edu.au:~/PatternAnalysis-2025/recognition/.../models/\*.png .


tail -f $(ls -t logs/*.log | head -1)
cat $(ls -t **/*.log 2>/dev/null | head -1)

alias sq='squeue -u $USER'
squeue -u $USER
squeue -u $USER -o "%.10i %.8j %.2t %.10M %.6D %R"

sbatch scripts/submit_job.sh
sbatch scripts/submit_job.sh both 512 10 1e-3 pretrained "" 10
sbatch scripts/submit_job_test.sh test 512 10 1e-3 pretrained <timestamp> 10

ls -lt logs/
find logs/ -type f -mtime +30 -exec rm -f {} \;

